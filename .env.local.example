# Database — Neon connection string (use POOLED url with -pooler in hostname)
DATABASE_URL="postgres://..."

# Vercel Blob
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# NextAuth — generate with: openssl rand -base64 32
AUTH_SECRET="..."
NEXTAUTH_URL="http://localhost:3000"

# Public env
NEXT_PUBLIC_SALESPERSON_URL="https://taylorproducts.net/meet-your-salesperson/"
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Initial admin (used by `npm run create-admin`)
ADMIN_EMAIL="matthew@adventii.com"
ADMIN_PASSWORD="change-me"
